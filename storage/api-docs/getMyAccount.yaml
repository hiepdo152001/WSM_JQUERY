get:
  summary: Get the current logged in user
  tags:
    - User
  security:
    - bearerAuth: []

  responses:
    200:
      description: "OK"
    500:
      description: "Server Error"
    401:
      description: "Unauthorized"
